# TODO — design refinement follow-through

_Source: `2026-05-20-spec-refine-summary.md` + `2026-05-20-rules-realignment.md`. Created 2026-05-20 after the multi-agent design review pass + the CLAUDE.md rule rewrite (new R2 = no half-built features, deleted R2 = old orphan-placeholder rule)._

**Execution order is load-bearing** — code cleanup BEFORE spec patching, otherwise the spec ends up describing UI surfaces that should have been deleted.

---

## Section A — Code cleanup sweep (do FIRST, before any spec edit)

Rationale: new R2 forbids half-built features. Existing disabled buttons / wire-up-later stubs are now violations. Spec patches in Section C would otherwise document surfaces that need to be removed, wasting the patch work.

### A.1 — REMOVE-PLACEHOLDER items (4 items, single PR each)

Goal for each: delete the disabled UI element, delete its now-orphaned support code, leave no `// TODO(T*)` references behind.

| # | What to remove | Files (starting points) | Notes |
|---|---|---|---|
| A.1.1 | Tag CRUD UI on Sessions | `src/sections/SessionsSection.tsx` + any `tag-*.tsx` under `src/components/` | Backend tag persistence: confirm not yet implemented; if backend code exists with no UI caller, that's also an R2 violation — delete or finish |
| A.1.2 | Sub-agents view (referenced in Plugin detail) | `src/sections/PluginsSection.tsx` or plugin-detail child component | Verify whether sub-agents tab is currently rendered or hidden — if rendered as disabled tab, remove tab + handler |
| A.1.3 | Plugins CRUD buttons (Install / Reinstall / Remove) — non-functional in current code per Plug-Skill refine F4/F5/F6 | `src/sections/PluginsSection.tsx`, `src/components/plugins/PluginCard.tsx` (if exists) | Update pill (amber "Update Available") — also remove until update flow is built end-to-end |
| A.1.4 | Settings section shell — currently 29-line placeholder with no real tab | `src/sections/SettingsSection.tsx` | Remove entire placeholder; remove sidebar nav entry + `Ctrl+,` shortcut wiring; restore once first tab (Appearance) is built end-to-end (see B.4) |

### A.2 — RENAME (1 item, trivial PR)

| # | What | Files |
|---|---|---|
| A.2.1 | Skills "Create Skill" button → "Open Skills Folder" (matches actual behavior — just opens File Explorer, doesn't scaffold) | `src/sections/SkillsSection.tsx` |

### A.3 — TODO sweep (housekeeping, can bundle into A.1.x commits)

Search the codebase for `// TODO(T<...>)` comments. For each:
- If the feature gets built in Section B → leave the TODO, it'll go away with the build
- If the feature gets removed in A.1 → delete the TODO along with the code
- If the feature isn't tracked anywhere → escalate to Ness with file:line

Search commands:
```bash
git grep -n 'TODO(T' src src-tauri
git grep -n 'wire up' src src-tauri
git grep -n 'deferred to' src src-tauri
```

---

## Section B — BUILD-NOW vertical slices (7 items, one PR each)

Rationale: per realignment file, these 7 are real features whose old fix was "add TODO + task ID". Under new R2 they must be built as full vertical slice (UI + backend together) or not exist at all.

**Each item is its own PR.** Don't bundle. Each PR must include: backend command + frontend caller + at-least one E2E smoke or unit test exercising the wire.

| # | Feature | Layers touched | Notes |
|---|---|---|---|
| B.1 | Quick Action: Resume latest session | Frontend button in Dashboard Quick Actions + backend session lookup IPC + `capabilities/default.json` entry | RCA Bug 3 territory — must actually work on click, not show toast "coming soon" |
| B.2 | Quick Action: Open CWD of latest session | Frontend button + reuse existing `open` shell IPC (verify capability allowlist already covers it) | If the shell-out is already allowed, capability change may be a no-op — verify |
| B.3 | Quick Action: Rebuild Stats | Frontend button + backend stats-rebuild IPC + invalidate dashboard store on success | Confirm what "stats" means against spec §4 — may already be derivable from existing data, in which case this is a refresh, not a rebuild |
| B.4 | Settings Appearance tab (vertical slice that re-introduces Settings shell removed in A.1.4) | New `appearance-store` (per T4-C3 decision: don't pollute theme-store) + Appearance tab UI + SQLite persistence in `src/lib/db.ts` SCHEMA[] + `EXPECTED_VERSION` bump + 1 MIGRATIONS entry | Once shipped, this resurrects the Settings section in the sidebar |
| B.5 | `get_cli_version` IPC | Rust command + frontend caller in Dashboard SystemHealth widget + capabilities entry | Tied to RCA Bug 4 close-out (SystemHealth source table) |
| B.6 | `read_claude_json` IPC for MCP project-scope configs | Rust command (read `~/.claude.json` keyed by project path) + frontend caller in MCP section + capabilities entry | Per DESIGN-CONTEXT — MCP configs live in `~/.claude.json`, not `settings.json` |
| B.7 | `write_project_mcp_json` IPC | Rust command + frontend MCP Add/Edit form wiring + capabilities entry | Per MCP refine: project-scope writes have NO IPC today; local-scope `cwd` also missing. Build both in this slice. Consolidates T3.12 |

---

## Section C — Spec patches (do AFTER A + B start landing)

Rationale: do after code cleanup so the spec doesn't describe surfaces that A removed. Order within Section C: highest-leverage first.

### C.1 — Tier 4 cross-section conflicts (need a decision before patching)

Three resolved already (recommendation Ness can accept by silence; any "no" overrides):
- **C1 `enabledPlugins` count semantics** → single shared selector exported from `plugin-loader.ts`
- **C2 shell-out policy** → new spec section §17.x covering trusted-dir + opt-in + error handling
- **C3 Appearance persistence** → new `appearance-store`, do NOT extend `theme-store`

Two unread (still inside `2026-05-20-spec-refine-summary.md` Tier 4):
- **C4** — needs to be read out of the summary
- **C5** — needs to be read out of the summary

**Todo:** read summary Tier 4, list C4 + C5 here with recommendation, then proceed.

### C.2 — Patch sequence (in order)

| Order | Patch | Closes | Source |
|---|---|---|---|
| 1 | §4.1 SystemHealth source-table + §4.4 R1-compliant Verification block | RCA Bug 4 (chart staleness via Verification escape clauses) | merger Patch #1 |
| 2 | §5 Sessions data model fix: separate `ALIVE (app-started)` vs `ALIVE (external)` in `SessionState` type | Dash-Sess S-F1 + S-F7 + spawnedByApp gap | Refine + cross-review confirmed |
| 3 | §5.3 ORPHANED redefinition (FS-walk as source of truth, not index-entry-without-JSONL) | DESIGN-CONTEXT §2.2 contradiction | Both refine + cross-review |
| 4 | §6 Plugins: rewrite §6.6 to drop never-built "tree view"; fix §6.5 cache-path segment count (wording only — confirm 3 vs 4) | Plug-Skill F1 + §6.6 | Cross-review noted F1 is wording quibble — verify before patching |
| 5 | §8 MCP: §17.10 form spec fix (project-scope save target contradicts Rust); §8.1 scope table; parser fixture contract for `claude mcp list` | MCP-Set top gaps | Cross-review confirmed grounded |
| 6 | §9 Settings: per-field file-of-truth map (Settings spans `settings.json` + `settings.local.json` + `config.json` + app SQLite) | MCP-Set §9 gap | Replaces 29-line placeholder |
| 7 | §17.5 error-table additions (5 missing rows: plugins-load-failed, skills-load-failed, update-check-failed, plugin-open-failed, skill-open-failed) | Plug-Skill cross-cut | Already in code, missing from spec |
| 8 | §17.6 loading-state self-contradiction audit (full audit, not row patches) | MCP-Set cross-cut | Both reviews flagged |
| 9 | Tier 1 (35 items) bulk patch in spec-section order | summary Tier 1 | Mechanical |
| 10 | Tier 2 (22 items) with cross-review modifications | summary Tier 2 | Mechanical with care |

### C.3 — Things to explicitly NOT do (Tier 3 rejects + invalidated findings)

- All 10 Tier 3 items in summary
- All 9 findings invalidated by realignment (8 INVALIDATED-but-WORSE — the underlying placeholder gets handled in Section A; 1 DROP because it cited deleted "pure store" rule)
- Don't add `CREATE TABLE` DDL to spec (TS owns schema)
- Don't mint `TBD-T*` task IDs in spec (spec flags wire-up needed; plan allocates ID)

---

## Section D — Plan-file updates (do alongside Section B)

For each B.x vertical slice, create a task entry in the relevant phase plan under `docs/superpowers/plans/` BEFORE starting the slice (not after — task ID needs to exist for commit message `feat(T<phase>.<num>): <subject>` per CLAUDE.md rule 3 in "Executing a plan task").

| Slice | Likely plan file | Suggested task ID range |
|---|---|---|
| B.1 / B.2 / B.3 (Quick Actions) | `2026-05-03-phase2-sessions-dashboard.md` or `2026-05-08-ui-defect-sweep.md` | Decide based on what's already in flight |
| B.4 (Settings Appearance) | `2026-05-03-phase4-dialogs-polish.md` | New T4.x |
| B.5 (`get_cli_version`) | `2026-05-03-phase2-sessions-dashboard.md` (Dashboard SystemHealth) | New T2.x |
| B.6 / B.7 (MCP IPCs) | `2026-05-03-phase3-plugins-skills-mcp.md` | Replaces / consolidates T3.12 |

---

## Section E — capabilities allowlist (do as part of each B.x slice)

Every new Tauri IPC introduced in Section B must add an entry to `src-tauri/capabilities/default.json` in the same PR. This is non-negotiable per architecture (webview cannot call un-allowlisted commands).

New IPCs introduced in Section B:
- `get_cli_version` (B.5)
- `read_claude_json` (B.6)
- `write_project_mcp_json` (B.7)

Plus whatever the Quick Actions in B.1–B.3 end up needing.

---

## Section F — Verify-then-clean checks (one-shots, low priority)

- [ ] Confirm `2026-05-20-rules-realignment.md` C4 and C5 by reading summary file Tier 4 in full
- [ ] Confirm whether `docs/research/2026-05-09-dashboard-bugs-rca.md` needs an addendum noting R2 was rewritten (RCA Bug 3 origin story now reads differently)
- [ ] Sweep `docs/superpowers/plans/*.md` for any task whose Verification block cites "R2" — those need re-wording
- [ ] Confirm `docs/DESIGN-CONTEXT.md` §20 (which CLAUDE.md now points at for R1/R3 rationale) actually exists and matches the new R1/R3 framing — if it still discusses old R2 (orphan-placeholder), update it

---

## Execution rules

- A and B are independent — A items can ship in parallel across multiple PRs; B items must be sequential per-feature but parallel across features
- C strictly waits until A is done (don't patch spec describing removed UI)
- Every B.x slice = one PR, must include: backend IPC + frontend caller + at least one test that exercises the bridge
- No `--no-verify`, no `it.skip`, no mocking the thing under test
- Commit format `feat(T<phase>.<num>): <subject>` for any work tied to a plan task; `chore:` / `fix:` / `refactor:` otherwise
- Each PR uses auto-pipeline (push → `gh pr create` → `gh pr merge --auto --squash --delete-branch`) per CLAUDE.md rule 9

---

## Status tracking

Use this checklist as the canonical status. Tick boxes as PRs merge.

### Section A
- [ ] A.1.1 Tag CRUD removed
- [ ] A.1.2 Sub-agents view removed
- [ ] A.1.3 Plugins CRUD buttons removed
- [ ] A.1.4 Settings shell removed (sidebar entry + Ctrl+, also removed)
- [ ] A.2.1 Skills button renamed
- [ ] A.3 TODO sweep complete

### Section B
- [ ] B.1 Quick Action: Resume latest session
- [ ] B.2 Quick Action: Open CWD
- [ ] B.3 Quick Action: Rebuild Stats
- [ ] B.4 Settings Appearance tab + appearance-store
- [ ] B.5 `get_cli_version` IPC
- [ ] B.6 `read_claude_json` IPC
- [ ] B.7 `write_project_mcp_json` IPC

### Section C
- [ ] C.1 Tier 4 conflicts decided (C1/C2/C3 recommendations recorded; C4/C5 read out and decided)
- [ ] C.2 Patches 1–10 applied in order
- [ ] C.3 Tier 3 rejects + invalidated findings explicitly not patched

### Section F
- [ ] F.1 Read C4 + C5 out of summary
- [ ] F.2 RCA addendum decision
- [ ] F.3 Plan-file R2 wording sweep
- [ ] F.4 DESIGN-CONTEXT §20 alignment check
