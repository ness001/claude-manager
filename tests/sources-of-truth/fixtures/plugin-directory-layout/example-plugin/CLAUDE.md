# Example Plugin

Synthetic CLAUDE.md file for the plugin-directory-layout fixture. Real plugins
ship a CLAUDE.md at the plugin root; Claude Code loads it at session start as
plugin-scoped context. The fixture test only asserts the file exists at the
root path; its contents are not parsed.
