---
name: foo-skill
description: "Synthetic skill used by the plugin-directory-layout source-of-truth fixture. Demonstrates the skills/<name>/SKILL.md layout with YAML frontmatter."
---

# Foo Skill

Body content for the synthetic example skill. The fixture test only reads the
YAML frontmatter above; this prose is here to mirror real SKILL.md files which
always have a body following the frontmatter.
